import { Router } from "express";

const authRouter = Router()

authRouter.post("/register", )
authRouter.get("/login")
authRouter.delete("/login")
authRouter.get("/refresh")
authRouter.get("/activate/:activateLink")

export default authRouter